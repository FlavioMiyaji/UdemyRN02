class Category {
    id: string;
    title: string;
    backgroundColor: string;
    color: string;

    constructor(id: string, title: string, backgroundColor: string, color: string) {
        this.id = id;
        this.title = title;
        this.backgroundColor = backgroundColor;
        this.color = color;
    }
}

export default Category;